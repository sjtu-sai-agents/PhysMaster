# Minimal runtime dependencies for PhysMaster agent pipeline (prior disabled)

# Core pipeline (run.py / clarifier / mcts / summarizer)
openai>=1.40.0
PyYAML>=6.0

# Optional LANDAU prior retrieval
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# Scientific computing
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0
matplotlib>=3.8.0
sympy>=1.12
mpmath>=1.3.0
gvar>=13.0
lsqfit>=13.0
vegas>=6.0
