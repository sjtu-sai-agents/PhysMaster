[USER REQUEST]

Subtask:
{subtask}

Path memory summaries:
{memory}

Node metadata:
{node_metadata}

Current node output path:
{path}

Execution notice:
- Treat the path above as the working output directory for this node.
- Any generated artifacts (plots/data/tables/scripts) must be written to that directory.
- Include concrete validation checks in your analysis whenever possible.
- Return only the required JSON object (no extra text).
